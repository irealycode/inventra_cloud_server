# Base URL of the cloud API. Leave empty to use same-origin (the default when
# FastAPI serves the built SPA). For dev with `npm run dev`, the Vite proxy in
# vite.config.ts forwards /api -> localhost:32950, so this can stay empty too.
VITE_API_BASE=
